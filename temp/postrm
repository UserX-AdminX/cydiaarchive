#!/bin/bash

dir="/var/unthreadedjb/"

if [ ! -e $dir ]; then
	exit 0
fi

rm -rf $dir 1>/dev/null 2>&1

echo "Please disconnect your device from your computer and reboot your device!"

declare -a cydia
cydia=($CYDIA)
if [[ ${CYDIA+@} ]]; then
    eval "echo 'finish:reboot' >&${cydia[0]}"
fi
